# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# 환경변수 파일 (보안상 절대 올리면 안됨!)
.env
.env.*
!.env.example  # 예시 파일은 올려도 됨

# IDE 설정 파일 (팀원끼리 설정 다를 수 있으므로 제외 추천)
.idea/
.vscode/
*.iml

# 도커 로그 등
*.log

# data folders
db_data/
db_data

# =========================
# 3. 프론트엔드 (Node.js / Next.js)
# =========================
# 의존성 모듈 (어느 폴더에 있든 다 무시)
node_modules/

# Next.js 빌드 결과물
.next/
out/

# 배포용 빌드 폴더
build/
dist/

# 패키지 매니저 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Next.js 타입스크립트 설정
next-env.d.ts
*.tsbuildinfo

# Vercel 배포 설정
.vercel

# =========================
# 4. 백엔드 (Spring Boot / Java / Gradle)
# =========================
# Gradle 캐시 및 설정
.gradle/

# 빌드 결과물 (class 파일, jar 파일 등)
/bin/
/out/
/build/

# 자바 클래스 파일 및 로그
*.class
*.log
*.ctxt
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# =========================
# 5. 기타 불필요한 파일
# =========================
Thumbs.db

# =========================
# 6. Jenkins (CI/CD)
# =========================
# 로컬에서 젠킨스를 돌릴 때 생기는 작업 폴더
.jenkins/
.jenkins/*

# 젠킨스 아티팩트 및 임시 파일
jenkins.war
*.jenkins.log
*.jenkins.err

# 도커로 젠킨스 띄울 때 로컬에 마운트할 수도 있는 데이터 폴더
jenkins_home/
jenkins_data/

# 주의: Jenkinsfile은 설정을 담은 코드이므로 깃허브에 '반드시' 올려야 합니다!
# (여기 적지 마세요!)